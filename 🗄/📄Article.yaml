title: string

short?: string

order:
  type: integer
  order: true
  init: true
  min: -1000
  max: 1000

content:
  type: document-markdown
  render:
    generateHeaderId: true
    autolink: blank
    typography: true

beta:
  type: boolean
  init: true

uid:
  type: document-uid
  path: true

summary:
  type: string
  optional: true

#hidden:
#  type: boolean
 # init: true